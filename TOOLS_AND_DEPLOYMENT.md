# チビちゃん＆パパ式 — ツール運用・公開・動作確認ルール v1.0

この文書は、現在および将来のすべてのアプリに共通して適用する、ツール選択・保存・公開・動作確認の運用ルールです。

目的は、AIが「コードを書いたところ」で仕事を終えず、可能な限り**ユーザーがすぐに変更結果を確認できるところまで**を一つの作業単位として扱うことです。

---

# 1. 基本方針

アプリ開発では、設計・実装・保存・マージ・デプロイ・公開・確認を別々の工程として扱う。

「コードを書いた」「GitHubへ保存した」「本番へデプロイした」「ユーザーが実際に触って確認できた」は同じ意味ではない。

AIは現在どの段階まで完了しているかを明確に区別して報告する。

特に、**本番URLまたは確認可能な成果物を実際に開ける状態になるまで、「公開完了」「反映済み」「確認済み」と断定しない。**

---

# 2. 各ツールの基本的な役割

## ChatGPT Chat

主に以下に使う。

- アイデア整理
- 仕様決定
- 小規模な修正
- UI・UX相談
- レビュー
- 問題の切り分け
- GitHubへの軽量な変更
- ユーザーとの確認

会話中に実行可能な変更は、必要以上にユーザーへ手作業を要求せず、可能な範囲でAIが実行する。

## ChatGPT Work

複数ファイルやリポジトリ全体にまたがる、まとまった作業に使う。

例：

- repo全体の調査
- 複数ファイルの一括修正
- 大規模なリファクタリング
- ブラウザでの実機テスト
- Private / Public混入チェック
- 回帰テスト
- 長時間・多工程の作業

小さな変更まで毎回Workへ移さず、作業規模に応じて使い分ける。

## GitHub

GitHubをコード・仕様・履歴の**正本**として扱う。

重要な変更はGitHubへ保存し、会話だけに残さない。

ただし、

**GitHubへ保存された = 公開された**

ではない。

コミット、マージ、デプロイ、公開、本番確認はそれぞれ別工程として扱う。

## Cloudflare

今後の新規Webアプリ・静的ツールでは、特別な理由がなければ第一候補の公開基盤とする。

特に以下の用途と相性がよい。

- 小規模Webアプリ
- 静的HTML
- 教育用ツール
- 実験アプリ
- 頻繁に更新するアプリ
- GitHub連携による自動デプロイ

GitHubへのpushから自動公開できる構成を優先する。

将来的に必要になった場合は、Workers、D1、R2、KV、Durable Objects等も利用候補とする。

## Netlify

既存のNetlifyアプリは、明確なメリットがない限り無理にCloudflareへ移行しない。

Netlifyは特に以下の場合に利用候補とする。

- 既存サイトが安定稼働している
- Deploy Previewが有用
- Netlify固有機能を利用する
- 移行コストや破損リスクの方が大きい

クレジット制のサービスでは、必要性の低いProduction Deployを繰り返さない。

---

# 3. 新規アプリの公開先

新しく作るWebアプリについて特別な理由がない場合、

**GitHub + Cloudflare**

を標準構成の第一候補とする。

一方、既存アプリは現在正常に動いている公開基盤を尊重し、理由なく移行しない。

---

# 4. Private / Publicの分離

個人開発用のアプリ・研究データ・個人的なルールと、第三者向けPublic Editionを混在させない。

Public版では原則として専用repository・専用公開URLを使用する。

Public版に以下を不用意に含めない。

- 個人的な会話履歴
- Private固有の名称
- 非公開研究データ
- 個人的なAI人格
- 個人用の例
- Private専用開発ルール

既存アプリのrepoやURLへ、無関係なPublicツールを便宜的に間借りさせない。

---

# 5. Public Kit等の導線

AI App Builder Kit等のPublicツールでは、利用者ができるだけ少ない操作で開始できる導線を優先する。

公開されたクリック式企画カルテが利用できる場合は、

**クリック式企画カルテ → Project Brief生成 → コピー → ChatGPTへ貼り付け**

を標準導線とする。

チャット内での質問形式Kickoffは、

- Web版を利用できない
- ユーザーがチャット形式を希望した

などの場合のfallbackとする。

AIが利用可能な企画カルテを無視して、最初から独自アンケートを開始しない。

---

# 6. 存在しないユーザー情報を作らない

ユーザーが明示していない過去の好み、経験、関心、発言を、

- 「以前言っていた」
- 「前から興味があった」
- 「以前希望していた」

などとして捏造しない。

過去情報を参照する場合は、現在の会話、提供された資料、実際に利用可能な記憶、接続された情報源のいずれかに存在する情報だけを使用する。

---

# 7. Clean-room Test

Public版は必要に応じて、過去文脈を持たない新しい環境でテストする。

特に以下を確認する。

- 初見でも開始方法が分かる
- Private版固有情報が混入していない
- 存在しない過去情報を捏造しない
- Free環境でも利用できる
- 重要な導線がモデルの推測に依存していない
- 外部ツールへのリンクが実際に開く
- スマートフォンでも操作できる

---

# 8. 更新後の「すぐ確認できる」ルール

これは重要な開発原則とする。

**変更を実装しただけでは作業完了とみなさない。**

可能な限り、更新後すぐにユーザーが結果を確認できる形まで整える。

優先順位は以下。

1. **公開中のWebアプリ**
   - 更新をデプロイし、変更箇所を直接確認できるクリック可能なURLをChatGPT上で提示する。

2. **Preview環境がある場合**
   - Preview URLを提示する。

3. **ローカル成果物の場合**
   - ChatGPTから直接開ける、またはダウンロードできる成果物リンクを提示する。

4. **GitHub上だけに存在する場合**
   - GitHub上の対象ファイル、PR、コミット等を可能な範囲で直接確認できる導線を提示する。

ユーザーに、

「GitHubを開いて、repoを探して、フォルダを開いて、このファイルを探してください」

というような不要な探索をさせない。

可能な限り、**ChatGPTの返答にあるリンクを1回押せば確認できる状態**を目指す。

---

# 9. 更新後の標準報告

変更後は、必要に応じて以下を短く報告する。

- 何を変更したか
- どこまで完了したか
- ユーザーがどこを押せば確認できるか

例：

> ボタン文言を変更し、GitHubへの反映とCloudflareへのデプロイまで完了しました。  
> ここから確認できます：［公開サイト］

逆に、本番確認までできていない場合は、

> GitHubへの反映までは完了しています。Cloudflareのデプロイ確認はまだです。

のように正確に伝える。

---

# 10. 公開状態の呼び分け

以下を明確に区別する。

- **実装済み** — コード上の変更が完了した。
- **保存済み** — GitHub等へ保存された。
- **マージ済み** — 対象branchへ統合された。
- **デプロイ開始済み** — 公開処理が開始された。
- **公開済み** — 公開先へ配信された。
- **確認済み** — 実際のURLまたは成果物で期待する動作を確認した。

AIは、確認していない状態を「確認済み」と表現しない。

---

# 11. 更新頻度と公開判断

ユーザーがすぐ試したい変更については、可能な範囲で迅速に確認環境を提供する。

ただし、

**確認するために必ずProductionへ公開する必要があるとは限らない。**

Preview環境、ローカル成果物、開発URL等で十分な場合はそれらを利用する。

Production Deployにコストやリスクがあるサービスでは、Previewを優先する。

一方、低コストに安全な自動デプロイが可能なアプリでは、細かな改善を迅速に反映できる構成を優先する。

---

# 12. この文書の中心原則

**AI側では、コード変更ではなく「ユーザーが変更を目で見て触れるところまで」を、可能な限り一回の作業単位として扱う。**
